import { createTheme } from "@mui/material";

const theme = createTheme();

export const UmpireStyles = {
  container: {
    marginTop: theme.spacing(2),
    marginBottom: theme.spacing(2),
  },
  item: {
    margin: "auto",
  },
};
